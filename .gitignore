# Compiled and binary files
*.o                 # Object files (Clang)
*.obj               # Object files (MSVC/Windows)
*.out               # Unix executables (Clang, if on Unix-like systems)
*.ifc               # Interface files (C++23 modules, Clang/MSVC)
*.ilk               # Incremental linker files (MSVC)
*.pdb               # Program database files (MSVC debug symbols)
*.dll               # Dynamic link libraries (MSVC/Windows)
*.so                # Shared objects (Clang, if on Unix-like systems)
*.dylib             # Dynamic libraries (Clang, if on macOS)
*.a                 # Static libraries (Clang)
*.lib               # Static libraries (MSVC/Windows)
*.exp               # Export files (MSVC linker)
*.idb               # Intermediate debug files (MSVC)

# Build directories
build/              # Common build folder (Ninja)
objs/               # Object file directory

# Ninja-specific files
.ninja_deps         # Ninja dependency file
.ninja_log          # Ninja build log
build.ninja         # Ninja build script (often generated by CMake)

# CMake-specific files (if using CMake with Ninja)
CMakeCache.txt      # CMake cache
CMakeFiles/         # CMake temporary directory
cmake_install.cmake # CMake install script

# Clang-specific files
*.pch               # Precompiled headers (Clang/MSVC)
*.gch               # Precompiled header output (Clang)

# OS-generated files
.DS_Store           # macOS Finder metadata
.DS_Store?
._*                 # macOS resource forks
.Spotlight-V100     # macOS Spotlight
.Trashes            # macOS Trash
ehthumbs.db         # Windows thumbnail cache
Thumbs.db           # Windows thumbnail cache

# Editor-specific files
*.swp               # Vim swap files
*.swo
*~                  # Temporary files from editors
.vscode/            # VS Code settings folder (e.g., launch.json, tasks.json)
*.code-workspace    # VS Code workspace files

# Logs and temporary files
*.log               # Log files
*.tmp               # Temporary files
tmp/                # Temporary directory
*.bak               # Backup files
*.tlog              # MSVC build tool logs
*.manifest          # MSVC manifest files
